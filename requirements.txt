streamlit
pandas
numpy
scikit-learn
joblib


# 数据处理与科学计算
pandas==2.0.3
numpy==1.24.3

# 可视化
matplotlib==3.7.2
seaborn==0.12.2

# 机器学习与模型持久化
scikit-learn==1.3.0
xgboost==2.0.0
lightgbm==4.1.0
joblib==1.3.2

# 数据库连接
sqlalchemy==2.0.21
pymysql==1.1.0

# 轻量级应用框架
streamlit==1.28.0

#说明：
pandas 和 numpy 是数据分析基础，版本选择 2.0 系列以保持与新版 Python 兼容。
scikit-learn 使用 1.3.0，该版本支持 ColumnTransformer 和 Pipeline 等常用功能。
xgboost 和 lightgbm 选用了近期稳定版，如果安装遇到网络问题，可换用国内镜像源。
streamlit 用于构建交互式预测界面，版本 1.28 是当前稳定版。
#如果你在本地运行 SQL 查询时使用了其他数据库驱动（如 psycopg2），请按需添加。

#常见问题：
若 xgboost 安装失败，可尝试使用 conda install -c conda-forge xgboost（如果你安装了 Anaconda）。
lightgbm 在 Windows 上可能需要先安装 Visual C++ 运行时，但通常 pip 安装即可。
如果使用 joblib 加载模型，确保 Python 版本与训练时一致，否则可能出现兼容性警告。
